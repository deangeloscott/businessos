# Editions

Use the full BusinessOS by default. Specialized editions are intentionally smaller generated distributions for users who want a bounded subsystem. They share the same core architecture and are not separate source codebases.
