# Action Quality

Actions require an executor, concrete target/output, dependencies, capability needs, risk/autonomy constraints, success criteria, verification, and fallback where capability is unavailable.
