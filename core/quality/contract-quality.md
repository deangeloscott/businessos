# Contract Quality

A contract is operational only when purpose, triggers, inputs, ordered executable process, outputs, fallbacks, and completion criteria are sufficiently explicit for a capable human or agent to execute without inventing missing business steps.
