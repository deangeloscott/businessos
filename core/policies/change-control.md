# Change Control Policy

Every material external mutation must identify target, before state where practical, intended action, executor, rollback path when practical, and verification plan.


Customer-facing mutations must also preserve pre-change text state and validate newly introduced claim candidates under `core/policies/customer-facing-mutations.md` before the change can be verified/closed.
