# Security and Isolation

Canonical business objects may not reference another business by default. Credentials are external. Raw evidence is copied into the workspace only when allowed/needed; SourceRecord preserves provenance and access scope.
