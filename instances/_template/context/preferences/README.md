# Preference Profiles

Optional canonical `PreferenceProfile` JSON objects live here. They customize allowed business/team/role/operator choices without overriding BusinessOS invariants, business/Brand requirements, task requirements, approvals, or factual/claim governance.

Use `scripts/upsert_preference_profile.py` to create/update profiles and `scripts/resolve_preferences.py` to inspect effective preferences.
